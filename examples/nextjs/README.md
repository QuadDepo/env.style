# env.style — Next.js example

```bash
pnpm install && pnpm dev
```

Open http://localhost:3000 — the tab favicon should render tinted green
(`color: { development: '#00ff00' }` in `next.config.ts`).
