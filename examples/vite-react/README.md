# env.style — Vite + React example

```bash
pnpm install && pnpm dev
```

Open http://localhost:5173 — the tab favicon should render tinted green
(`color: { development: '#00ff00' }` in `vite.config.ts`).
